# 既定はテキストとして自動正規化（リポジトリ内は LF）。
* text=auto eol=lf

# シェルスクリプト・Dockerfile は必ず LF（CRLF だと Linux で動かない）。
*.sh    text eol=lf
bin/gh  text eol=lf
Dockerfile      text eol=lf
*.Dockerfile    text eol=lf

# PowerShell スクリプトは CRLF
*.ps1   text eol=crlf

# バイナリ
*.png   binary
*.gif   binary
